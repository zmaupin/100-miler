node_modules
dist
.env
.env.local
.netlify
*.local
.DS_Store
coverage

# Vendored third-party skill (install via plugin marketplace; not app code)
.claude/skills/impeccable/

# impeccable working dir (critique snapshots, live config) — local tool state
.impeccable/
